[tool.ruff]
line-length = 110
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B"]
ignore = [
    "B008", # FastAPI relies on function calls (Depends, File, Query) in argument defaults
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
